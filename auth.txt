
Authentication System 

1.Authentication is very imp part of any application. By this way we can ensure that system access given to 
authorized user.

How to Design and Implement the system?
How it works with real world Application?

Along with that we have to covered 
1. Access token 
2. Refresh token
3. Token Blacklisting
4. Token Rotation
5. OTP Based Verification
6. Logout System
7. Logout from all Devices

All the features implement with practical examples.


Authentication 



Client                                                 Server (Insta)
                                                        /api/posts/like/:pId
User A 

User B  ---------------------------------> register rqst , perform any operatons/access services on server don't give it unitl register.
            --------------------> share the details and server saved in DB.
            ----------------> MongodB save and it give you the id.(16byte)
            --> After server create token and store in user browser cookies by response.
            --> In token we have user details like username, or email or id.

User C 

By this way sever understand  which user hit the api.


Identify the user rqst come from which user --> Authentication.

--> How server know rqst come with which user.

1.First user --> Register --> To access service / feature/
        --> By this token server identify the user.

        In Every rqst user sent the token, then if 100 rqst , 1lakh it will sent.

        In that token detail of the user --> like id by which server understand the user. if info correct then access given.

        After some time it will expire. 

In other Device login then it will give the token.

--> Check the token is same.

Also understand the Access token and Refresh token.

wildCard Ip --> 0.0.0.0/0 only for development not use for production

test1

test2  --> by using the token of test1 it can login test1 dashboard.

if we have banking server then it transfer the money from the test1 to test2, becuase test user use the token of test1 
Server think rqst come from the tes1 user. And sent the data in respnse.

In Bank application 
amount deduct from the test1 who rqst and credit to test2.

Solution. 
--> How to store the token in the frontend.

// To store the token at client side.

// local storage --> client side par js we can read it. --> Attacker run script and get the data.

// cookies --> technically store --> http only , client side js not able to access but problem is that

create a form , action ="/api/accoount/transaction:xyx" method=POST --> direct access

Solution --> token in memory
Problem --> Page reload --> then clear --> token lose.(Not Persistent)

--> Not theft here.

We can use 2 token.

1.Access token --> It will treat as normal token treat. --> server know about who will rqst.
2.Refresh token --> Memory --> when page reload then run time data gone. 
// for that specific api create --> 

/api/auth/refresh --> return --> refresh token --> store in cookis.
// With the help of Refersh token we create the access token.


// transcation time we don't use refresh token. Not access the access token from the memory.
// session.


// logout from all the system

access token user authenticate on server.

logout --> Maintain sessionId , user generally has max to max 2-4 devices.
For every device we maintain the session 

session
{
        userid,
        refreshToken, --> store hash
        ip,
        userAgent,
        createdAi,
        updatedAi,
        }

Bydefault revoke value is false. 
if revoke true not

But access token is not expire , by using this we can login , --> blacklist in redis.
Jwt statlesss then we don't have to take on db.
But in case of redis , where big-giant company , tradeoff --> where time lookup then response, and cost 
tradeoff.